#include <bits/stdc++.h>

using namespace std;
long long int numb;
std::vector<int> v;
long long int tmp;
string s;

int main()
{
    cin>>numb;
    for(long long int i=0;i<num;i++){
        getline(cin,s);
        stringstream ss(s);
        while(s >> tmp){

        }
        for(long long int i=0;i<s.length();i++){
            if (s.at(i)!=' '){
                v.push_back(48-s.at(i));
            }
        }


    }

    
    return 0;
}
